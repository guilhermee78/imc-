<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalculadoraDeIMC"
        tools:targetApi="31">

        <!-- Tela inicial do app -->
        <activity
            android:name="com.example.calculadoradeimc.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demais Activities (TELAS) -->
        <activity
            android:name=".FormularioIMCActivity"
            android:exported="false" />

        <activity
            android:name=".CalculoImcActivity"
            android:exported="false" />

        <activity
            android:name=".CadastroUsuarioActivity"
            android:exported="false" />

        <activity
            android:name=".ListaUsuariosActivity"
            android:exported="false"/>

        <activity
            android:name=".AtualizarUsuarioActivity"
            android:exported="false"/>

    </application>

</manifest>
